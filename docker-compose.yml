version: '3.2'

services:
  resurface:
    image: docker.resurface.io/release/pilot:2.2.5
    container_name: express_resurface
    ports:
      - '4000:4000'
      - '4001:4001'
      - '4002:4002'

  express:
    image: test-express
    container_name: express
    command: node index.js
    ports:
      - '80:5000'
    env_file:
      - .env
